#include <stdio.h>

// Saída de Dados

int main() {

    printf("Olá, mundo!\n");

    // Imprimindo variáveis
    int inteiro = 10;
    float real = 15.6;
    //double real = 16.5;
    char caracter = 'A';
    char cadeia[50] = "Pedro";

    // Códigos de formatação
    printf("Inteiro: %d\n", inteiro);
    printf("Real: %f\n", real);
    printf("Caracter: %c\n", caracter);
    printf("Cadeia: %s\n", cadeia);

    // Questões: Tipos de Dados, Variáveis e Saída de Dados 

    return 0;
}